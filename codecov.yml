coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 60%

comment:
  layout: "reach,diff,flags,components"
  behavior: default

flags:
  flutter:
    paths:
      - apps/client/
    carryforward: true
