name: record_linux
description: Stub — voice recording not supported on Linux desktop.
version: 0.7.0
environment:
  sdk: ^3.11.0
  flutter: ^3.41.0
dependencies:
  flutter:
    sdk: flutter
  record_platform_interface: ^1.0.0

flutter:
  plugin:
    implements: record
    platforms:
      linux:
        dartPluginClass: RecordLinux
